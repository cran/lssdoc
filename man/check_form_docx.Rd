% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_form_docx.R
\name{check_form_docx}
\alias{check_form_docx}
\alias{print.lss_form_check}
\title{Report every problem of a Word authoring form at once}
\usage{
check_form_docx(path)

\method{print}{lss_form_check}(x, ..., n = 20L)
}
\arguments{
\item{path}{Character. Path of the \code{.docx} form to check.}

\item{x}{An \code{lss_form_check} object.}

\item{...}{Ignored.}

\item{n}{Maximum number of problems to print; \code{Inf} for all.}
}
\value{
An object of class \code{lss_form_check}: a data frame with one row per
problem and the columns \code{severity} (\code{"error"} or \code{"warning"}), \code{class}
(the condition class), \code{block}, \code{code}, \code{field} and \code{message}. Zero rows
means the document reads: \code{\link[=read_form_docx]{read_form_docx()}} will return a specification.
A \code{print()} method summarizes it.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\strong{Experimental.} Run \code{\link[=read_form_docx]{read_form_docx()}}'s parser in dry-run mode: an
author iterates on a form until it is clean, and fixing one problem per
round trip is not a workflow. The same rules, the same messages; the first
error simply does not stop the read.

A problem in one block does not hide the problems of the next: parsing
resumes at the following block. Three problems are still fatal, because
nothing can be read past them: an unreadable file, a missing or
unsupported contract version, and a document-wide structural refusal
(tracked changes, content controls). When one of those fires it is the only
row reported.
}
\examples{
if (requireNamespace("officer", quietly = TRUE) &&
    requireNamespace("flextable", quietly = TRUE)) {
  form <- tempfile(fileext = ".docx")
  lss_template_docx(form, lang = "fr", kinds = c("single", "text"))
  check_form_docx(form)
}
}
\seealso{
\code{\link[=read_form_docx]{read_form_docx()}}, \code{\link[=write_form_docx]{write_form_docx()}}, \code{\link[=lss_template_docx]{lss_template_docx()}}.
}
