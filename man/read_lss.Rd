% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_lss.R
\name{read_lss}
\alias{read_lss}
\title{Read a LimeSurvey \code{.lss} file}
\usage{
read_lss(file)
}
\arguments{
\item{file}{Character. Path to a \code{.lss} file. Must be a single string
pointing to an existing file, otherwise a classed error is raised
(\code{lssdoc_bad_path}, \code{lssdoc_file_not_found}).}
}
\value{
An object of class \code{lss}: a list with the survey languages,
metadata, and one data frame per \code{.lss} section. Structural sections
(\code{surveys}, \code{groups}, \code{questions}, \code{subquestions}, \code{answers},
\code{question_attributes}, \code{conditions}) stay separate from the
localized text sections (\code{survey_language_settings}, \code{group_l10ns},
\code{question_l10ns}, \code{answer_l10ns}), which carry the per-language
titles, labels, and help texts. All values are read verbatim as
character.
}
\description{
Read a LimeSurvey survey structure export (\code{.lss}, an XML file) and turn
it into a structured \code{lss} object that the rest of the package can
audit (\code{\link[=audit_lss]{audit_lss()}}) and render (\code{\link[=render_questionnaire]{render_questionnaire()}},
\code{\link[=render_audit]{render_audit()}}). Parsing is fully local: the file is never uploaded
anywhere.
}
\details{
The \code{.lss} format is a LimeSurvey XML export. Since DBVersion 4xx/7xx
the translatable text lives in dedicated localization sections
(\verb{*_l10ns}), keyed by language, while the structural sections hold
identifiers and settings. \code{read_lss()} reads every section into a tidy
data frame without mutating any user-facing identifier or text. A
field that is present but empty (e.g. \verb{<help/>}) is read as \code{""}; a
field that is absent from a row is read as \code{NA}.
}
\examples{
# A synthetic four-language demo survey ships with the package.
demo <- system.file("extdata", "demo_survey.lss", package = "lssdoc")
lss <- read_lss(demo)
lss$languages
}
