% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_questionnaire.R
\name{render_questionnaire}
\alias{render_questionnaire}
\title{Render a LimeSurvey questionnaire to a Word or PDF document}
\usage{
render_questionnaire(
  input,
  output,
  languages = NULL,
  template = c("cards", "table"),
  layout = c("auto", "side-by-side", "stacked"),
  show_audit = TRUE,
  show_help = TRUE,
  show_attrs = c("prefix", "suffix", "other_replace_text", "validation"),
  show_technical_attrs = FALSE,
  page_format = c("auto", "A4-portrait", "A4-landscape", "A3"),
  show_toc = TRUE,
  show_index = TRUE,
  show_quotas = TRUE,
  show_header_title = TRUE,
  show_source = TRUE,
  show_item_heading = FALSE,
  show_raw_filter = FALSE,
  show_groups = TRUE,
  show_welcome = TRUE,
  show_endtext = TRUE,
  show_description = TRUE,
  show_consent = TRUE,
  show_privacy_settings = FALSE,
  show_admin_settings = FALSE,
  title = NULL,
  logo = NULL,
  logo_width = 1.5,
  logo_height = 0.75,
  font = NULL,
  font_code = NULL,
  colors = NULL,
  authors = NULL,
  description = NULL,
  chrome_lang = NULL,
  variable_names = c("brackets", "underscore"),
  base_size = 10L
)
}
\arguments{
\item{input}{Either a path to a \code{.lss} file (character string) or a
pre-parsed \code{lss} object returned by \code{\link[=read_lss]{read_lss()}}. Passing a path
parses it on the fly. Passing an \code{lss} object avoids re-parsing
when the same survey is rendered with different options (e.g.
multiple language subsets) in the same session.}

\item{output}{Character. Path to the file to create. The extension
determines the output format: \code{.docx} writes a Word document
directly, \code{.pdf} writes a Word document into a temporary location
and converts it locally via LibreOffice (or Word, on Windows).
Any other extension is rejected with \code{lssdoc_bad_output_ext}.}

\item{languages}{Character vector of language codes to display,
\strong{in the order they will appear as columns}. \code{NULL} (default)
keeps all languages found in the \code{.lss} file in the order of the
\verb{<languages>} section. Acts both as a subset filter and an
ordering: e.g. \code{c("en", "fr")} puts English first, \code{c("fr", "en")}
puts French first. \code{languages[1]} is treated as the primary
language (TOC entries, group fallback). Requesting a language
absent from the survey is an error (\code{lssdoc_unknown_language}).}

\item{template}{Output style. One of \code{"cards"} (the default) or
\code{"table"}.
\itemize{
\item \code{"cards"} renders one detached pair of tables per item (meta
table + item table), stacked vertically, with content
languages displayed side-by-side in the item table.
\item \code{"table"} renders a single dense table covering the whole
document: every variable is one tinted Question row carrying
\code{No | Variable | Type | Mand. | Filter}, followed by one or
more white Value rows. Group banners (and a group's
description, when the author wrote one) become section rows;
the column header repeats on every page. The body font
steps down for three or four languages so the columns stay
within the portrait content width.
}}

\item{layout}{Reserved for future use. Currently \code{"auto"} only.}

\item{show_audit}{Logical. If \code{TRUE} (default), include an audit
summary section near the top and inline markers on questions
that carry findings. Set to \code{FALSE} for a clean reading copy.}

\item{show_help}{Logical. If \code{TRUE} (default), include question
help texts under the question text.}

\item{show_attrs}{Character vector of question attributes to surface
under the question text when present. Default keeps the attributes
that change how respondents see the item: \code{"prefix"}, \code{"suffix"},
\code{"other_replace_text"}, \code{"validation"}. Add \code{"exclude_all_others"}
or \code{"exclude_all_others_auto"} to also surface the row-level
exclusivity flags (debug-style). Pass \code{character(0)} to hide all.}

\item{show_technical_attrs}{Logical. If \code{TRUE}, include technical
attributes such as \code{answer_order} and \verb{location_*}. \code{FALSE} (the
default) hides them.}

\item{page_format}{Page format. One of \code{"auto"} (the default),
\code{"A4-portrait"}, \code{"A4-landscape"}, or \code{"A3"}. \code{"auto"} is
template-aware and never follows the language count (four languages
fit on A4 portrait): the \code{"cards"} template gets A4 portrait, while
the dense \code{"table"} template gets A4 landscape, which it needs to
stay readable. Pass an explicit value to override; selecting a wider
page makes every panel (meta table, item table, audit/quota tables
and the dense table) expand automatically to fill the
content width, in both templates.}

\item{show_toc}{Logical. If \code{TRUE} (default), include a table of
contents listing the groups (skipped automatically when the
survey has fewer than two groups). For per-variable navigation,
use \code{show_index}.}

\item{show_index}{Logical. If \code{TRUE} (default), append a variable
index at the end of the document listing every item code with
its number, sorted alphabetically.}

\item{show_quotas}{Logical. If \code{TRUE} (default), append a quotas
section (after the end text, before the variable index) listing
each sampling quota: its localized name, status (active, limit and
action when full), the membership condition resolved to question
codes and answer labels, and the localized "quota full" message.
Skipped when the survey defines no quotas.}

\item{show_header_title}{Logical. If \code{TRUE} (default), show the
survey title at the top right of every page (one line per
displayed language, truncated to 80 characters). \code{FALSE} keeps
only the \code{X/Y} page counter at the bottom right.}

\item{show_source}{Logical. If \code{TRUE} (default), show the
\strong{Source file} name and \strong{Survey ID} rows in the cover
metadata table. Pass \code{FALSE} to hide both (e.g. when sharing
without exposing the LimeSurvey internals).}

\item{show_item_heading}{Logical. If \code{FALSE} (the default), the
meta table starts each item directly, for a compact layout. If
\code{TRUE}, a bold \code{"N. variable"} heading is added above each item
for scroll-time navigation.}

\item{show_raw_filter}{Logical. If \code{FALSE} (the default), the
Filter cell shows only the human-readable form (e.g.
\code{Q1 = 1}) -- editorial style, matching ESS / MOSAiCH /
GESIS conventions. Set to \code{TRUE} to also surface the raw
LimeSurvey relevance expression underneath in small italic
gray (e.g. \code{!is_empty(Q1.NAOK) && (Q1.NAOK == 1)}), useful for
QA cross-checks. The raw form is always shown when the plain
form could not be simplified.}

\item{show_groups}{Logical. If \code{TRUE} (default), show the group
banners (cards layout) or group rows (table layout). Pass
\code{FALSE} to flatten the document into a single sequence of items
with no section breaks (useful when groups exist only as
internal organization).}

\item{show_welcome}{Logical. If \code{TRUE} (default), include the
survey's multilingual welcome text (\code{surveyls_welcometext}) as
a side-by-side block (cards) or embedded row (table).}

\item{show_endtext}{Logical. If \code{TRUE} (default), include the
survey's multilingual end text (\code{surveyls_endtext}). Same
treatment as \code{show_welcome}.}

\item{show_description}{Logical. If \code{TRUE} (default), include the
survey's multilingual description (\code{surveyls_description}) --
the "what this survey is about" intro that LimeSurvey shows
above the welcome text on the landing page.}

\item{show_consent}{Logical. If \code{TRUE} (default), render a data
protection and consent block in the front matter (before the
welcome text): the survey's privacy policy notice
(\code{surveyls_policy_notice}) and its consent checkbox label
(\code{surveyls_policy_notice_label}), side by side across languages,
with the checkbox drawn as an empty box. Skipped when the survey
turns the policy notice off or carries no notice text.}

\item{show_privacy_settings}{Logical. If \code{FALSE} (the default),
omit the survey-level privacy / tracking flags from the cover.
Set to \code{TRUE} to surface \code{anonymized}, \code{save} partial,
\code{datestamp}, \code{ipaddr}, and \code{refurl} rows -- useful for ethics
committee submissions.}

\item{show_admin_settings}{Logical. If \code{FALSE} (the default),
omit the survey-level administrative settings. Set to \code{TRUE}
to surface \code{alias}, end URL with description, and \code{active}
flag rows.}

\item{title}{Optional override of the survey title shown on the
cover and the top-right header. \code{NULL} (default) uses the
per-language titles from the \code{.lss} survey settings. Pass a
single string to use the same title in every displayed
language, or a named character vector keyed by language code
(e.g. \code{c(fr = "Mon titre", de = "Mein Titel")}) for per-language
overrides.}

\item{logo}{Optional path (character) to a PNG or JPEG image
displayed at the top of the cover page. \code{NULL} (default) keeps
the cover logo-free, matching the neutral style of
survey-methodology references (ESS, MOSAiCH, Panel). The \code{.lss}
file does not embed a logo, so this image must be supplied by
the caller.}

\item{logo_width, logo_height}{Image dimensions in inches.
Defaults \code{1.5} and \code{0.75}, tuned to a 2:1 logo. Resize or
pre-crop your image to fit a different aspect ratio.}

\item{font}{Optional body font name (character). \code{NULL} (default)
keeps Calibri, which is pre-installed on every recent Windows
Office and metric-substituted with Carlito (OFL) on Mac and
Linux LibreOffice, so column widths stay stable across
platforms. Pass any string to override (e.g. \code{"Source Sans 3"},
\code{"IBM Plex Sans"}, or a corporate brand font); install the font
on the machine that opens the document, otherwise the reader's
application substitutes its own fallback face.}

\item{font_code}{Optional monospace font name (character) used
for code-like content: the variable column in each meta table,
the raw relevance expression under each filter cell, and the
variable index entries. \code{NULL} (default) keeps Consolas; pass
\code{"JetBrains Mono"} or \code{"IBM Plex Mono"} for sharper code style.}

\item{colors}{Optional named list of hex color overrides for the
editorial petrol-blue palette. \code{NULL} (default) keeps the
package palette intact. Accepted names: \code{"primary"} (group
filets, headers text and item top borders), \code{"accent"}
(hyperlinks, ORCID iD, URL auto-links, group under-line in
cards), \code{"band"} (light header backgrounds), \code{"band_dark"}
(the meta-table dark header in cards), \code{"zebra"} (the
very-light tint on Question rows in the \code{table} template),
\code{"grid"} (the 0.5 pt border color), \code{"text"}, \code{"muted"}. Each
value must be a hex string (\code{"#XXXXXX"} or \code{"#XXX"}). Unknown
keys are rejected (\code{lssdoc_bad_colors}). Useful for honoring an
institutional brand: e.g.
\code{colors = list(primary = "#5C9F1A", accent = "#7FA82E")}
produces a LimeSurvey-green document.}

\item{authors}{Optional credit block for the questionnaire's
designers, displayed on the cover page below the subtitle.
Each author is shown centered on its own line as
\code{Name -- Affiliation}; when an ORCID iD is provided, a smaller
monospace line below shows \verb{ORCID 0000-0000-0000-0000} as a
hyperlink to \verb{https://orcid.org/<id>}. Accepts:
\itemize{
\item \code{NULL} (default): no authorship block.
\item An \strong{unnamed character vector} (\code{c("Jane Doe", "John Doe")}):
each entry becomes a line with no affiliation.
\item A \strong{named character vector}
(\code{c("Jane Doe" = "HESAV")}): names are authors,
values are affiliations. Use \code{""} to render an author without
affiliation.
\item A \strong{list of named lists} for the full form, e.g.
\code{list(list(name = "Jane Doe", affiliation = "HESAV", orcid = "0009-0001-2345-6789"), list(name = "John Doe", affiliation = "HESAV"))}. The \code{name} field is required;
\code{affiliation} and \code{orcid} are optional.
}}

\item{description}{Optional free-form text (single string) shown
on the cover page below the authors block. \code{NULL} (default)
omits the block. Useful for a citation hint, a funding
acknowledgement, a methodology note, or a link to a related
publication. Line breaks (\verb{\\n}) split the block into separate
centered lines; \verb{http://} and \verb{https://} tokens are rendered
as clickable hyperlinks.}

\item{chrome_lang}{Language used for the \strong{chrome} of the document
(column headers, row labels, navigation titles, type labels,
Value descriptors, audit section). One of \code{"en"}, \code{"fr"},
\code{"de"}, \code{"es"}, \code{"it"}. \code{NULL} (default) follows \code{languages[1]}
when supported, otherwise falls back to \code{"en"}. Independent
from \code{languages}, which controls the survey's content columns:
e.g. \code{chrome_lang = "en"} with \code{languages = c("fr", "en")}
produces an English-labelled document with French and English
content. Spanish and Italian translations should be reviewed
by a native speaker before publishing an official document.}

\item{variable_names}{How response-variable names are written, so the
document matches the data file the reader holds. One of:
\itemize{
\item \code{"brackets"} (default) -- the exact column names of the
\strong{CSV / Excel data export}, so the variable index reproduces the
raw data file column for column: \code{parent[subq]},
\code{parent[subq][1]} (dual scale), \code{parent[CH]} / \code{parent[other]}
(multiple choice), \code{parent[59842]} (ranking, by answer id),
\verb{parent[_Ccomment]} (list-with-comment).
\item \code{"underscore"} -- the sanitized code form used by the Expression
Manager / relevance equations and the SPSS / Stata / R exports
(\code{parent_subq}, \code{parent_subq_1}).
The two-dimensional arrays (array of numbers / texts with a second
axis) and ranking questions are expanded so every produced column
appears as its own entry either way.
}}

\item{base_size}{Body type size in points (default \code{10}). One lever
scales the whole document: question text, item tables, the meta band,
the quotas table, the variable index and the cover metadata all
follow it, while headings and answer/help text keep their relative
offsets. Useful for a roomier single-language render (e.g. \code{12}). The
cover title and subtitle keep their fixed title-page sizes. Accepted
range: 7 to 16.}
}
\value{
The \code{output} path, invisibly.
}
\description{
Build a professional questionnaire document from a LimeSurvey survey,
displaying up to four languages side by side. Each question becomes a
compact \code{flextable} with a meta header (variable code, type, mandatory,
filter) shown once, language column headers, the question text per
language, and the subquestion or answer-option rows underneath -- codes
on the left, labels per language on the right. Headings, a metadata
cover page, an optional table of contents, and an optional audit summary
tie the document together. Rendering uses the suggested packages
\pkg{officer} and \pkg{flextable}; both must be installed.
}
\section{"LimeSurvey last save" date on the cover}{

The cover metadata table carries a row labelled
\emph{"LimeSurvey last save"} (or its localized equivalent). It is read
verbatim from the \code{surveys.lastmodified} column of the \code{.lss},
which is the only timestamp LimeSurvey writes into the export --
no other table (\code{questions}, \code{question_l10ns}, \code{answer_l10ns},
\code{groups}, etc.) carries a per-row modification date. The row is
named "last save" rather than "last modified" because LimeSurvey
only bumps that field reliably when the user clicks \strong{Save} on a
survey-level form (Settings tab); editing a question text, an
answer label, or a translation through the Question Editor does
\strong{not} consistently update it across LimeSurvey versions. If the
date looks stale relative to your most recent edits, the
workaround is to open \emph{Survey settings} in LimeSurvey, click
\strong{Save} (no other change needed), then re-export the \code{.lss}. The
next render will show the bumped timestamp.
}

\section{Field-update prompt in Word}{

Opening the rendered \code{.docx} in Microsoft Word may surface a
security-style prompt: \emph{"This document contains fields that may
refer to other files. Do you want to update the fields in this
document?"}. This is expected: the package marks the page-number
and bookmark-reference fields as needing a refresh so the footer
shows the correct page count and the table of contents links
resolve to the right pages on first open (this is also what makes
headless PDF conversion via LibreOffice produce correctly
paginated output without a manual F9). Clicking \strong{Yes} is safe --
the document has no \code{INCLUDETEXT}, \code{INCLUDEPICTURE}-linked, or DDE
fields; the only external links are the ORCID and DOI URLs in the
cover credits, which are static \code{HYPERLINK} targets and not fetched
on update.
}

\section{PDF output}{

When \code{output} ends in \code{.pdf}, the function first renders a \code{.docx}
to a temporary location and then converts it locally via
LibreOffice headless (or Word on Windows). LibreOffice
(\code{soffice} executable) must be installed and on \code{PATH}; otherwise
a classed error explains how to install it. Conversion stays on
the user's machine: no upload, no network call. LibreOffice
headless does not refresh Word field values (TOC, page counts)
during conversion, so the table of contents may appear empty in
the converted PDF. To obtain a PDF with a populated TOC, render to
\code{.docx} instead, open it in Word (the TOC refreshes automatically)
and use \verb{File > Save As > PDF}.
}

\examples{
\dontrun{
file <- system.file("extdata", "demo_survey.lss", package = "lssdoc")

# One-shot: parse + render Word document
render_questionnaire(file, tempfile(fileext = ".docx"))

# Same call, PDF output (format inferred from extension)
render_questionnaire(file, tempfile(fileext = ".pdf"))

# Parse once, render several variants without re-parsing
lss <- read_lss(file)
render_questionnaire(lss, tempfile(fileext = ".docx"),
                     languages = "en")
render_questionnaire(lss, tempfile(fileext = ".docx"),
                     template = "table",
                     languages = c("en", "fr"))

# Branded cover with authors block and palette override
render_questionnaire(
  lss,
  tempfile(fileext = ".docx"),
  template    = "table",
  chrome_lang = "en",
  colors      = list(primary = "#5C9F1A", accent = "#7FA82E"),
  authors     = list(
    list(name = "Jane Doe", affiliation = "HESAV",
         orcid = "0009-0001-2345-6789"),
    list(name = "John Doe", affiliation = "HESAV",
         orcid = "0009-0002-3456-7890")
  )
)
}
}
\seealso{
\code{\link[=render_audit]{render_audit()}} for the audit-only document;
\code{\link[=audit_lss]{audit_lss()}} to inspect findings in the console without
rendering; \code{\link[=read_lss]{read_lss()}} to pre-parse a \code{.lss} file once and
render multiple variants.
}
