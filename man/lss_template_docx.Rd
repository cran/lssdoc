% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_form_docx.R
\name{lss_template_docx}
\alias{lss_template_docx}
\title{Write a blank Word template for authoring a questionnaire}
\usage{
lss_template_docx(path, lang = "fr", kinds = lss_kinds$kind, languages = lang)
}
\arguments{
\item{path}{Character. Path of the \code{.docx} file to write.}

\item{lang}{Language of the template's labels and example wording, one of
\code{"en"}, \code{"fr"} (default), \code{"de"}, \code{"es"}, \code{"it"}.}

\item{kinds}{Character vector of kinds to illustrate, \code{lss_kinds$kind}
(all 21) by default; the order of the kind table is kept.}

\item{languages}{Content languages of the questionnaire to be written,
the first one being the primary language. Defaults to \code{lang}, giving a
single-language form. With several, every text row is repeated once per
language, keyed \code{Wording [fr]}, \code{Wording [en]} and so on, and the reader
then requires each of them: a questionnaire with a missing translation
is what \code{\link[=audit_lss]{audit_lss()}} exists to catch. Independent of \code{lang}, which only
sets the language of the form's own labels.}
}
\value{
Invisibly, the path to the written file.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\strong{Experimental.} Generate the blank authoring form: one example
question per requested kind, every default pre-filled, and a muted hint
under each key telling the author the syntax the field expects. The
template is always \strong{generated} from the kind table -- the package ships
no static \code{.docx} -- so it cannot describe a field \code{\link[=lss_spec]{lss_spec()}} would
refuse.

Delete the example questions you do not need, edit the others, then hand
the file to the reader (0.3.0) to obtain an \code{\link[=lss_spec]{lss_spec()}} and, through
\code{\link[=write_lss]{write_lss()}}, a \code{.lss} file LimeSurvey imports.

The template is the render of a generated example specification, so the
same object feeds the template, the tests and the vignette. The value of a
Type row is always the spec kind (\code{single}, \code{array5}, ...); the localized
type label is shown as the hint under the key, because it is many-to-one
and could not be read back. Reserved words
are localized: an option line reading \code{Autre} in a French template creates
the native other option, and an ordinary option that happens to read
"Autre" is written with an explicit code (\code{9 = Autre}) -- which the hint
under the Options key spells out.

Requires the suggested packages \pkg{officer} and \pkg{flextable}.
}
\examples{
if (requireNamespace("officer", quietly = TRUE) &&
    requireNamespace("flextable", quietly = TRUE)) {
  out <- tempfile(fileext = ".docx")
  lss_template_docx(out, lang = "fr", kinds = c("single", "multiple"))
  file.exists(out)

  # A bilingual questionnaire, with French labels on the form itself.
  both <- tempfile(fileext = ".docx")
  lss_template_docx(both, lang = "fr", languages = c("fr", "en"),
                    kinds = "single")
  file.exists(both)
}
}
\seealso{
\code{\link[=write_form_docx]{write_form_docx()}} to render an existing specification,
\code{\link[=lss_spec]{lss_spec()}}, \code{\link[=write_lss]{write_lss()}}.
}
