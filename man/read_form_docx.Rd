% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_form_docx.R
\name{read_form_docx}
\alias{read_form_docx}
\title{Read a Word authoring form back into a survey specification}
\usage{
read_form_docx(path)
}
\arguments{
\item{path}{Character. Path of the \code{.docx} form to read.}
}
\value{
An \code{\link[=lss_spec]{lss_spec()}} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\strong{Experimental.} Parse a \code{.docx} \strong{authoring form} -- the document
\code{\link[=write_form_docx]{write_form_docx()}} and \code{\link[=lss_template_docx]{lss_template_docx()}} produce, filled in by an
author in Word -- back into an \code{\link[=lss_spec]{lss_spec()}}, ready for \code{\link[=write_lss]{write_lss()}} and a
LimeSurvey import. The round trip is exact: a specification written as a
form and read back describes the same survey.

Reading needs no suggested package: only \pkg{xml2} and \code{\link[utils:unzip]{utils::unzip()}},
so an author's file can be turned into a \code{.lss} on a bare installation.
\pkg{officer} and \pkg{flextable} are needed to WRITE a form, never to read
one.

The contract the document must honor -- all of it written by
\code{\link[=write_form_docx]{write_form_docx()}}, and spelled out in the blank template's hints:
\itemize{
\item one top-level two-column table per block (Survey, Group, Question,
Quota), the key on the left and the value on the right; the first row of
a block names it, and a title row starts a new block even in the middle
of a table (pasting a block makes Word merge two tables).
\item keys are matched by their TEXT, ignoring case, accents, quotes and a
trailing colon, against the labels of all five chrome languages, so a
French author can fill an English template. Only the first line of a key
cell is read: the muted hints under the key are ignored.
\item a value cell holds one value per line, where a line is a paragraph or a
soft return (Enter or Shift+Enter). Options, rows and columns are read as
\code{code = label}, or as a bare label that gets auto-numbered; the reserved
word (\code{Other}, \code{Autre}, \code{Sonstiges}, \code{Otro}, \code{Altro}) alone or on the
left of \code{=} is the native other option.
\item the Type cell carries the kind code (\code{single}, \code{array5}, ...); a
localized type label is accepted only when it names exactly one kind.
\item \code{Mandatory} takes the yes/no words of any language (and \code{y}/\code{n},
\code{true}/\code{false}, \code{1}/\code{0}); blank means no. The \code{Filter} cell takes the
mini-language of \code{\link[=lss_spec]{lss_spec()}} (\code{Q1 = 1}, \verb{Q2 in [1, autre]},
\code{count(Q3) >= 2}); its keywords are matched whatever Word capitalized,
and the case of a question or answer code is never changed.
\item when the survey declares several languages, every localizable key is
suffixed with a language code -- \code{Wording [fr]}, \code{Wording [en]} -- and
every declared language must supply every text.
\item the file must carry the custom document property
\code{lssdoc-template-version}: it is the reader's proof that the document
agreed to this contract. Tracked changes, content controls, merged cells,
a third column, a nested table and Word's automatic list numbering are
each refused with a classed error naming the block and the field.
}

Errors carry the class \code{lssdoc_bad_form} plus one leaf class
(\code{lssdoc_bad_form_file}, \verb{_marker}, \verb{_layout}, \verb{_block}, \verb{_key}, \verb{_value},
\verb{_spec}), and every message names the block, the question code and the
field. Use \code{\link[=check_form_docx]{check_form_docx()}} to list every problem of a document at once
instead of stopping at the first.
}
\examples{
if (requireNamespace("officer", quietly = TRUE) &&
    requireNamespace("flextable", quietly = TRUE)) {
  form <- tempfile(fileext = ".docx")
  lss_template_docx(form, lang = "fr", kinds = c("single", "text"))
  spec <- read_form_docx(form)
  spec
}
}
\seealso{
\code{\link[=write_form_docx]{write_form_docx()}} and \code{\link[=lss_template_docx]{lss_template_docx()}} to write the form,
\code{\link[=check_form_docx]{check_form_docx()}} for a dry run, \code{\link[=lss_spec]{lss_spec()}}, \code{\link[=write_lss]{write_lss()}}.
}
