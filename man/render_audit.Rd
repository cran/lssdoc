% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_audit.R
\name{render_audit}
\alias{render_audit}
\title{Render the audit as a focused Word or PDF document}
\usage{
render_audit(
  input,
  output,
  languages = NULL,
  logo = NULL,
  logo_width = 1.5,
  logo_height = 0.75,
  font = NULL,
  font_code = NULL,
  colors = NULL,
  authors = NULL,
  description = NULL,
  chrome_lang = NULL
)
}
\arguments{
\item{input}{Either a path to a \code{.lss} file (character string) or a
pre-parsed \code{lss} object returned by \code{\link[=read_lss]{read_lss()}}. Passing a path
parses it on the fly; passing an \code{lss} object avoids re-parsing in
a workflow that already inspected the audit.}

\item{output}{Character. Path to the file to create. The extension
determines the output format: \code{.docx} writes a Word document
directly, \code{.pdf} writes a Word document into a temporary location
and converts it locally via LibreOffice (or Word, on Windows).
Any other extension is rejected with \code{lssdoc_bad_output_ext}.}

\item{languages}{Character vector of language codes used on the
cover page. \code{NULL} (default) keeps all languages of the survey in
their declared order.}

\item{logo}{Optional path (character) to a PNG or JPEG image
displayed at the top of the cover page. \code{NULL} (default) keeps
the cover logo-free.}

\item{logo_width, logo_height}{Image dimensions in inches. Defaults
\code{1.5} and \code{0.75}, tuned to a 2:1 logo. Resize or pre-crop your
image to fit a different aspect ratio.}

\item{font}{Optional body font name (character). \code{NULL} (default)
keeps Calibri. See \code{\link[=render_questionnaire]{render_questionnaire()}} for guidance on
overrides.}

\item{font_code}{Optional monospace font (character) used for
code-like content (variable codes, raw expressions). \code{NULL}
(default) keeps Consolas.}

\item{colors}{Optional named list of hex color overrides for the
editorial petrol-blue palette. \code{NULL} (default) keeps the
package palette intact. Same shape and accepted names as in
\code{\link[=render_questionnaire]{render_questionnaire()}}.}

\item{authors, description}{Optional cover-page credit block
(\code{authors}) and free-form note (\code{description}). \code{NULL} (default)
for both. Same shapes as in \code{\link[=render_questionnaire]{render_questionnaire()}}.}

\item{chrome_lang}{Language used for the document chrome (column
headers, row labels, audit section). One of \code{"en"}, \code{"fr"},
\code{"de"}, \code{"es"}, \code{"it"}. \code{NULL} (default) follows \code{languages[1]}
when supported, otherwise falls back to \code{"en"}.}
}
\value{
The \code{output} path, invisibly.
}
\description{
Build a short, action-oriented document containing only the audit
findings: the same cover page as the full questionnaire document,
summary counts, then one table per severity (errors, warnings,
notes) listing every finding with its location and message. Use it
for QA follow-up or to share issues with a colleague without
distributing the full questionnaire.
}
\examples{
\dontrun{
# One-shot (path -> .docx)
render_audit(
  system.file("extdata", "demo_survey.lss",
              package = "lssdoc"),
  tempfile(fileext = ".docx")
)

# PDF output -- same call, just pass a .pdf path
render_audit("survey.lss", "qa.pdf")
}
}
\seealso{
\code{\link[=audit_lss]{audit_lss()}} to inspect the same findings in the console;
\code{\link[=render_questionnaire]{render_questionnaire()}} for the full questionnaire document.
}
